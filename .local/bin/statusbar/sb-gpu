#!/bin/sh
echo "GPU:  $(cat /sys/class/drm/card*/device/gpu_busy_percent)%  $(($(cat /sys/class/drm/card*/device/hwmon/hwmon*/temp1_input) / 1000))°C  $(($(cat /sys/class/drm/card*/device/mem_info_vram_used) / 1024 / 1024))MiB"
