#!/bin/sh

# ########
# Main
# ########

swap=$(free -h | awk '/Swap: / {print $3}')

# Source theme
. ~/.local/bin/statusbar/themes/gruvbox-dark.sh

# Display with theme
[ -n "$swap" ] && echo "$theme  $swap $reset"
