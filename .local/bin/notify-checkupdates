#!/bin/sh

TERMINAL_EMULATOR=alacritty
DELIVERY_OPTIMIZATION=delivery-optimization@tp-x13

# helper function
delivery_optimization_journal() {
  nohup "$TERMINAL_EMULATOR" -e journalctl -u "$DELIVERY_OPTIMIZATION" -f >/dev/null 2>&1 &
  echo $!
}

notify_update() {
  action=$(dunstify --action="update,Update packages" --timeout=60000 "📦 Pending updates: $(echo "$UPDATES" | wc -l)" "$UPDATES")

  if [ "$action" = "update" ]; then
    if [ -n "$DELIVERY_OPTIMIZATION" ]; then
      pid=$(delivery_optimization_journal)
      sudo systemctl start "$DELIVERY_OPTIMIZATION"
      kill "$pid"
    fi

    "$TERMINAL_EMULATOR" -e sh -c "sudo pacman -Syu --noconfirm; yay -Sua --noconfirm; echo -n ':: Press enter to quit'; read"
  else
    exit 1
  fi
}

notify_reboot() {
  action=$(dunstify --action="reboot,Reboot system" --timeout=60000 "🔄 Reboot the system" "Kernel modules get removed by default. Install aur/linux-keep-modules to fix it.\nIf you do, next time you will not need to reboot.")

  if [ "$action" = "reboot" ]; then
    systemctl reboot
  fi
}

# only support the following kernels
contains_kernel() {
  echo "$UPDATES" |
    while read -r line; do
      case "$line" in
      'linux') return 0 ;;
      'linux-lts') return 0 ;;
      'linux-zen') return 0 ;;
      *) return 1 ;;
      esac
    done
}

# cleaner syntax
linux_keep_modules() {
  pacman -Qqs | grep -q 'linux-keep-modules'
}

# main
if UPDATES="$(checkupdates --nocolor)"; then
  notify_update
  if contains_kernel && ! linux_keep_modules; then
    notify_reboot
  fi
fi
