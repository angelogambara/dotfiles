# Move custom-hourly.timer and custom-hourly.service in /etc/systemd/system/
# then, run systemctl daemon-reload and enable the timer.

[Unit]
Description=My hourly timer

[Timer]
OnCalendar=*-*-* *:00:00
Persistent=true

[Install]
WantedBy=timers.target
